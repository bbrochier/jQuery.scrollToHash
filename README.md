jQuery.scrollToHash
===================

Customizable Scroll to hash jQuery plugin

##Todo

###Options

- offset management
- scroll speed
- element si pas de #

##Usage
```
	$('a').scrollToHash();
```

##Options
```
	$('a').scrollToHash({
        scrollSpeed : 4000 //default:1000
    });
```